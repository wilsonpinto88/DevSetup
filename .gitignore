# Logs & generated status files (regenerated by scripts, not source)
installation-progress.log
installation-status.txt

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS noise
Thumbs.db
.DS_Store
desktop.ini

# Editor
.vscode/*.log

# Never commit credentials (belt-and-braces; these shouldn't live here anyway)
*.credentials.json
.credentials.json
*.pem
*.key

# Old Bootstrap Superpowers doc + diagram images -- kept locally for personal reference,
# not tracked (describes a capability no longer part of this repo)
*.pptx
*.docx
Bootstrap_Superpowers_Documentation.md
Bootstrap_Superpowers_Executive_Summary.md
images/
generate_images.py

# winget list snapshot -- personal reference for non-winget apps, not needed by any script
# (winget-packages.json already automates reinstalling everything winget-manageable)
Installed-Apps.txt

# AI assistant artifacts
context-snapshot.json

# Workspace-local VS Code settings -- may contain machine/location-specific paths;
# each clone/location keeps its own, not shared via git
.vscode/settings.json

# --- Note for agents opening this repo at a new location ---
# This repo was moved from "OneDrive - Unipartner IT Services, S.A\Documents\DevSetup"
# to "C:\Users\Wilson.Pinto\DevSetup" (2026-07-16). Portability comes from git, not OneDrive sync.
# Scripts/ and AgentSetup/ use relative paths / $PSScriptRoot / $env:USERPROFILE -- no hardcoded
# absolute paths as of that date. If you find one referencing the old OneDrive path, it's stale --
# flag it, don't assume it's still valid.
